<view class="container">
  <view wx:for="{{users}}" wx:for-item="user" wx:key="{{user.id}}" class="content">
    <view class="header">
      <image src="{{user.avatar_url}}"></image>
      <text class="date-section">{{user.wechat_name}}</text>
    </view>
  </view>
</view>